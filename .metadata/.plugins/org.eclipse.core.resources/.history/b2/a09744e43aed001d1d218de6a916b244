package com.lec.ex02_board;

/*
    A. DAO / DTO(VO)
    
    1. DAO: Data Access Object 
    
      DAO는 간단히 DB의 data에 접근을 하기 위한 객체로서 DB에 접근을 하기 위한 로직과
      비지니스(select, delete ..) 로직을 분리하는 개념이다. 
      
      DAO는 DB에 접근해서 데이터를 조회하거나 조작하는 기능을 전담하도록 만든 객체이다.
      사용자는 자신이 필요한 Interface를 DAO에 전달하고 DAO는 이 인터페이스를 구현한 객체를
      사용자가 편리하게 사용할 수 있도록 반환해 준다.
    
    2. DTO(VO): Data Transfer Object, (Value Object)
    
    B. 게시판 Application
    
*/

public class Board {

	public static void main(String[] args) {
		
		BoardMenu menu = new BoardMenu(1.0);
		menu.mainMenu();

	}

}
